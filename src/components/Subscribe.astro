---
// Subscribe.astro — email subscribe block.
// Uses MailerLite's classic embedded-form POST endpoint (no external JS).
// Gated on SITE.newsletter.{accountId,formId}; falls back to RSS + Kill-the-Newsletter CTA when unset.
import { SITE } from '../consts';

interface Props {
  variant?: 'card' | 'inline';
  heading?: string;
}
const { variant = 'card', heading = 'Subscribe' } = Astro.props;

const hasEmail =
  SITE.newsletter.provider === 'mailerlite' &&
  SITE.newsletter.accountId.length > 0 &&
  SITE.newsletter.formId.length > 0;

// MailerLite classic embedded-form endpoint. Accepts CORS POSTs from any origin
// and redirects to MailerLite's confirmation page.
const actionUrl = hasEmail
  ? `https://assets.mailerlite.com/jsonp/${SITE.newsletter.accountId}/forms/${SITE.newsletter.formId}/subscribe`
  : '';
---
<aside class={`subscribe subscribe--${variant}`} aria-labelledby="subscribe-heading">
  <div class="inner">
    <h2 id="subscribe-heading" class="title">{heading}</h2>
    <p class="blurb">{SITE.newsletter.blurb}</p>

    {hasEmail && (
      <form action={actionUrl} method="post" class="form">
        <label class="sr-only" for="ml-email">Your email</label>
        <input
          type="email"
          name="fields[email]"
          id="ml-email"
          required
          placeholder="you@example.com"
          autocomplete="email"
        />
        <input type="hidden" name="ml-submit" value="1" />
        <input type="hidden" name="anticsrf" value="true" />
        <button type="submit">Subscribe</button>
      </form>
    )}

    <p class="alt">
      {hasEmail ? 'Prefer a feed reader? ' : 'Follow via '}
      <a href="/rss.xml">RSS</a>
      {' · '}<a href="https://kill-the-newsletter.com/" rel="noopener">Email-via-RSS</a>
      {SITE.social.twitter && <>{' · '}<a href={SITE.social.twitter} rel="me noopener">X / Twitter</a></>}
    </p>
  </div>
</aside>

<style>
  .subscribe {
    margin: 2.5rem 0 0;
    padding: 1.6rem 1.4rem;
    border: 1px solid var(--c-rule);
    border-radius: var(--radius-lg);
    background: var(--c-bg-tint);
  }
  .subscribe--inline {
    margin-top: 3rem;
  }
  .inner { max-width: 34rem; margin: 0 auto; }
  .title {
    font-family: var(--font-display);
    font-weight: 400;
    font-variation-settings: 'opsz' 72;
    font-size: 1.35rem;
    margin: 0 0 .4rem;
    letter-spacing: -0.01em;
  }
  .blurb {
    font-family: var(--font-body);
    font-size: .95rem;
    color: var(--c-text-soft);
    margin: 0 0 1rem;
    line-height: 1.5;
  }
  .form {
    display: flex;
    gap: .5rem;
    flex-wrap: wrap;
  }
  .form input[type="email"] {
    flex: 1 1 16rem;
    font-family: var(--font-ui);
    font-size: .95rem;
    padding: .65rem .8rem;
    border: 1px solid var(--c-rule);
    border-radius: var(--radius);
    background: var(--c-bg);
    color: var(--c-text);
  }
  .form input[type="email"]:focus {
    outline: 2px solid var(--c-accent);
    outline-offset: 1px;
    border-color: var(--c-accent);
  }
  .form button {
    font-family: var(--font-ui);
    font-size: .92rem;
    font-weight: 600;
    padding: .65rem 1.2rem;
    border: 1px solid var(--c-accent);
    border-radius: var(--radius);
    background: var(--c-accent);
    color: var(--c-bg);
    cursor: pointer;
    transition: filter .15s ease;
  }
  .form button:hover { filter: brightness(1.08); }
  .alt {
    font-family: var(--font-ui);
    font-size: .82rem;
    color: var(--c-muted);
    margin: .9rem 0 0;
  }
  .alt a { color: var(--c-text-soft); }
  .alt a:hover { color: var(--c-accent); }
  .sr-only {
    position: absolute; width: 1px; height: 1px; padding: 0; margin: -1px;
    overflow: hidden; clip: rect(0,0,0,0); white-space: nowrap; border: 0;
  }
</style>
